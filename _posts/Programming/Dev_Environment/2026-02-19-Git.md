---
title: "[개발 환경] Git"
date: 2026-02-19 00:00:00 +/-TTTT
categories: [Programming, 개발 환경]
tags: [개발 환경]
math: true
toc: true
author: sunho
---

## Git이란?

Git은 내 컴퓨터에서 코드의 모든 변경 사항을 기록하고 관리하는 버전 관리 소프트웨어이다.

### Git vs GitHub

**Git**은 컴퓨터에 설치하는 소프트웨어다.
<br>
즉, <span style="background-color:#fff5b1">내 컴퓨터 (로컬)에서 작동하는 프로그램</span>이기 때문에, 인터넷이 끊겨도 문제 없이 코드를 수정하고, 버전을 저장할 수 있다.

**GitHub**는 온라인에 존재하는 웹 서비스로, <span style="background-color:#fff5b1">원격 저장소를 제공하는 클라우드 서비스</span>이다.
<br>
즉, 로컬에서 Git으로 작업한 내용을 안전하게 백업하거나, 다른 팀원들과 협업하기 위해 인터넷 상에 올려놓을 수 있다.

따라서 Git을 이용해 다른 사람들과 협업할 때는, 먼저 내 컴퓨터에서 Git으로 코드를 작업하고 그 결과를 GitHub에 올려서 공유하는 방식으로 개발한다.

### Git 기능

Git에는 여러 기능이 있으며, 이를 이용하면 프로젝트의 추적과 분업 및 협업이 수월해진다.

기본적으로 <span style="background-color:#fff5b1">History 기록, Branch 작업, Merge 기능은 네트워크 연결 없이 로컬에서만 독립적</span>으로 이루어진다.
<br>
반면, <span style="background-color:#fff5b1">Remote 기능은 로컬에서 완성된 작업 내역을 깃허브 같은 원격 저장소와 동기화할 때만 사용</span>된다.

### History

코드의 변경 사항을 시간 순으로 기록하는 기능이며, 단순히 파일을 저장하는 것이 아니라 '누가, 언제, 어떤 이유로' 코드를 수정했는지 상세히 기록한다.
<br>
이 덕분에 실수가 발생했을 때 언제든 과거의 특정 시점으로 되돌릴 수 있다.

기록을 남기는 최소 단위를 commit이라고 하며, History는 이 commit들이 모여 만들어진다.

| 명령어 | 설명 | 사용 예시 |
| :---: | :---: | :---: |
| `git status` | 현재 수정된 파일이나 Staging Area의 상태를 확인 | - |
| `git add <파일명>` | 수정된 파일을 스냅샷을 찍기 위한 Staging Area에 올림 <br> (모든 수정 파일을 올리려면 `git add .`) | `git add script.py` |
| `git commit -m "<설명>"` | Staging Area에 있는 파일들을 하나의 스냅샷으로 기록 | `git commit -m "initial commit"` |
| `git log --oneline` | 지금까지의 commit 이력을 한 줄씩 간결하게 출력 | - |

### Branch

하나의 프로젝트에서 Main Branch를 바탕으로 독립적인 작업 공간을 분리하는 기능이다.

메인 코드에 영향을 주지 않고 새로운 기능을 실험하거나 버그를 수정할 수 있는 일종의 평행 세계를 제공하며, 이 덕분에 하나의 프로젝트 안에서 여러 가지 실험을 독립적으로 진행할 수 있다.

| 명령어 | 설명 | 사용 예시 |
| :---: | :---: | :---: |
| `git branch` | 로컬에 존재하는 브랜치 목록 출력 <br> (원격 저장소의 branch까지 모두 출력하려면 `git branch -a`) | - |
| `git branch <브랜치명>` | 로컬에서 새로운 브랜치 생성 | `git branch dev` |
| `git switch <브랜치명>` | 해당 브랜치로 작업 공간 이동 | `git switch dev` |
| `git branch -d <브랜치명>` | 작업이 끝난 브랜치 삭제 | `git branch -d dev ` |

`git branch -a` 명령어를 실행하면 아래와 같은 화면이 출력될 것이다.

![fig1](Programming/Dev_Environment/Git-1.png){: style="display:block; margin:0 auto; width:60%;"}

출력에 대한 의미는 다음과 같다.

- 빨간색 텍스트는 원격 저장소에 존재하는 브랜치, 흰색 텍스트는 로컬에 존재하는 브랜치, `*` 초록색 텍스트는 현재 내가 위치해 있는 브랜치를 의미한다.
- `origin`은 코드가 저장된 원격 저장소 (깃허브 서버)의 별명이다.

    원래 원격 저장소의 URL `https://github.com/...`을 매번 입력하기는 번거롭기 때문에, Git에서 자동으로 `origin`이라는 별명을 붙여준 것이다.
- `remotes/origin/HEAD -> origin/main`은 원격 저장소를 대표하는 기본 브랜치 (HEAD)가 원격 저장소의 `main` 브랜치라는 뜻이다.

    우리가 `git clone`으로 레포지토리를 복제할 때, Git은 화살표 `->`가 가리키는 곳 (원격 저장소의 HEAD)을 확인하고 자동으로 해당 브랜치를 기준으로 로컬 환경을 초기 세팅한다.
- `*` 기호는 현재 내가 위치해 있는 브랜치, 즉 내 로컬의 HEAD를 의미한다.

    우리가 `git branch`로 로컬에서 새로운 브랜치를 생성할 때, Git은 `*`가 표시된 곳 (로컬의 HEAD)을 확인하고 현재 작업 중인 코드를 기준으로 새로운 브랜치를 생성한다.
- `main` (또는 `master`)은 기본 브랜치, 그 외의 `dev`나 `test1` 등의 브랜치는 사용자가 직접 생성한 브랜치이다.

    현재 `dev`는 원격 저장소에, `test1`과 `test2`는 로컬에 존재하는 것을 확인할 수 있다.

### Merge

각기 다른 브랜치에서 작업한 결과물을 다시 하나의 메인 코드로 합치는 기능이다.

| 명령어 | 설명 | 사용 예시 |
| :---: | :---: | :---: |
| `git merge <브랜치명>` | 현재 내가 위치한 브랜치로 <br> `<브랜치명>`의 작업을 가져와 통합 | `git merge test1` |

#### Merge 오류

`git merge`를 실행했을 때, 상황에 따라 나타나는 경고 또는 에러 메시지에는 크게 3가지가 있다.

- **Merge Conflict (병합 충돌)**

    두 브랜치에서 같은 파일의 같은 line을 각각 다르게 수정했을 때, Git이 스스로 어떤 코드를 선택해야 할지 몰라서 사용자에게 도움을 요청하는 상태이다.

    ```bash
    CONFLICT (content): Merge conflict in <파일명>
    Automatic merge failed; fix conflicts and then commit the result.
    ```

    충돌이 난 파일을 열어보면 `=======`를 기준으로, `<<<<<<< HEAD`와 `>>>>>>>` 기호로 두 코드가 나뉘어 있다. 
    <br>
    남길 코드만 선택하고 제거할 코드와 기호들을 지운 뒤, 다시 `git add`와 `git commit`을 하면 병합이 완료된다.

    ![fig3](Programming/Dev_Environment/Git-3.png){: style="display:block; margin:0 auto; width:60%;"}

- **Uncommitted Changes (커밋하지 않은 변경 사항 존재)**

    현재 내가 작업 중인 브랜치에 아직 커밋하지 않은 수정 사항이 남아있는데 병합을 시도할 때 나타나는 경고다. 
    <br>
    기존 작업물이 날아가는 것을 방지하기 위한 Git의 안전장치다.

    ```bash
    error: Your local changes to the following files would be overwritten by merge:
    Please commit your changes or stash them before you merge.
    ```

    하던 작업을 `git commit`으로 완전히 저장한 뒤, 다시 병합을 시도하면 된다.

- **Unrelated Histories (관련 없는 기록 병합 거부)**

    서로 출발점이 다른 두 프로젝트를 강제로 합치려고 할 때 나타나는 경고다. 
    <br>
    보통 로컬에서 프로젝트를 새로 만들고, 원격 저장소에서도 새 레포지토리를 만들어서 둘을 연결하려 할 때 자주 발생한다.

    ```bash
    fatal: refusing to merge unrelated histories
    ```

    만약 두 프로젝트를 억지로라도 하나로 합쳐야 한다면, 강제 옵션을 붙인 명령어 `git merge <브랜치명> --allow-unrelated-histories`를 실행한다.

### Remote

내 컴퓨터에서 기록한 History와 Branch들을 원격 저장소에 올려서 팀원들과 공유하고, 반대로 팀원들이 올린 새로운 업데이트를 내 컴퓨터로 가져와 동기화하는 기능이다.

| 명령어 | 설명 | 사용 예시 |
| :---: | :---: | :---: |
| `git fetch` | 원격 저장소의 최신 업데이트를 로컬에 다운로드 (작업 코드에는 영향 X) | - |
| `git push origin <브랜치명>` | 내 컴퓨터의 commit들을 깃허브로 업로드 | `git push origin dev` |
| `git pull origin <브랜치명>` | 원격 저장소의 최신 업데이트를 로컬에 다운로드한 뒤 병합 | `git pull origin dev` |

- `git pull`은 `fetch`와 `merge`가 순차적으로 일어난다고 생각하면 된다.

#### Push 오류

`git push`를 실행했을 때, 상황에 따라 나타나는 경고 또는 에러 메시지에는 크게 3가지가 있다.

- **원격 저장소에 나보다 최신 코드가 있는 경우**

    **<span style="color:red">원인:</span>** 내가 `push`하려는 코드보다 원격 저장소의 코드가 더 최신 상태일 때 발생한다. (예: 팀원이 먼저 해당 브랜치에 `push`를 했거나, 내가 깃허브 웹에서 파일을 직접 수정한 경우)
    <br>
    이때 Git은 내 코드가 덮어씌워지는 것을 막기 위해 `push`를 거부한다.

    ```bash
    ! [rejected] main -> main (fetch first)
    error: failed to push some refs to 'https://github.com/...
    hint: Updates were rejected because the remote contains work that you do not have locally.
    ```

    **<span style="color:blue">해결 방법:</span>** 먼저 `git pull`을 해서 원격 저장소의 최신 변경 사항을 내 컴퓨터로 가져와 병합한 뒤에, 다시 `git push`를 해야 한다.

- **권한이 없거나 인증에 실패한 경우**

    **<span style="color:red">원인:</span>** 해당 원격 저장소에 코드를 밀어 넣을 쓰기(Write) 권한이 없거나, 로컬 리눅스/터미널 환경에서 계정 인증(SSH 키 또는 토큰)이 제대로 설정되지 않았을 때 발생합니다.

    ```bash
    remote: Permission to user/repo.git denied to user.
    fatal: unable to access '...': The requested URL returned error: 403
    (또는) Permission denied (publickey).
    ```

    해결 방법: 협업 중이라면 깃허브 레포지토리의 Collaborator로 등록되어 있는지 확인해야 합니다. 터미널 환경이라면 로컬에 등록된 SSH 키(~/.ssh/id_rsa.pub)가 깃허브 계정에 잘 등록되어 있는지, 혹은 Personal Access Token(PAT)이 만료되지 않았는지 점검해야 합니다.

- **푸시하려는 브랜치가 존재하지 않을 때**

error: src refspec main does not match any
error: failed to push some refs to '...'

원인: 존재하지 않는 브랜치 이름을 푸시하려고 했거나(오타), 프로젝트를 처음 만들고 아직 git commit을 한 번도 하지 않은 빈 깡통 상태에서 푸시를 시도할 때 발생합니다.

해결 방법: git branch를 입력해 현재 브랜치 이름이 맞는지 확인하고, 초기 세팅 중이라면 커밋 기록이 하나 이상 존재하는지 확인합니다.

#### Pull 오류

`git pull`은 다운로드는 잘 되더라도, 주로 병합하는 과정에서 오류가 잦다.

- 커밋하지 않은 내 작업물이 덮어씌워질 위험이 있을 때

error: Your local changes to the following files would be overwritten by merge:
Please commit your changes or stash them before you merge.

원인: 원격 저장소에서 가져온 코드가 '내가 현재 로컬에서 수정 중이되, 아직 커밋하지 않은 파일'과 겹칠 때 발생합니다. Git이 내 작업물이 날아가는 것을 막아준 것입니다.

해결 방법: 현재 하던 작업을 git commit으로 저장하거나, git stash로 잠시 치워둔 뒤에 다시 pull을 받으면 됩니다.

- 충돌(Conflict)이 발생했을 때

CONFLICT (content): Merge conflict in {파일명}
Automatic merge failed; fix conflicts and then commit the result.

원인: git merge를 직접 쳤을 때와 완전히 동일합니다. 팀원이 원격에 올려둔 코드와 내가 로컬에서 수정한 코드가 같은 줄을 건드렸을 때 발생합니다.

해결 방법: 충돌난 파일을 열어 코드를 직접 수정한 뒤, git add와 git commit을 진행하여 병합을 마무리합니다.

- 백그라운드 프로세스 충돌 (index.lock 에러)

fatal: Unable to create '/작업폴더/.git/index.lock': File exists.

원인: CLI나 리눅스 환경에서 자주 겪는 현상입니다. Git 명령어가 백그라운드에서 아직 돌고 있거나(예: IDE가 자동으로 git 상태를 갱신 중일 때), 이전 Git 프로세스가 비정상 종료되면서 index.lock이라는 잠금 파일을 지우지 못한 채 남아있을 때 발생합니다.

해결 방법: 터미널에서 rm -f .git/index.lock 명령어로 해당 잠금 파일을 수동으로 삭제해 준 뒤 다시 pull을 시도하면 정상 작동합니다.

![fig1](Programming/Dev_Environment/Git-1.png){: style="display:block; margin:0 auto; width:50%;"}
_[[출처]](https://forum.inductiveautomation.com/t/perspective-component-building-horizontal-git-branch/66513)_

## Git을 통한 협업

로그인 기능을 담당하는 **개발자 A**와 장바구니 기능을 담당하는 **개발자 B**가 하나의 쇼핑몰 프로젝트를 함께 만드는 상황을 가정해 보자.
<br>
두 사람이 각각 `main` 코드에서 자신의 브랜치를 파서 작업을 시작했을 때 발생할 수 있는 상황에 대해 살펴보자.

### 서로 다른 파일을 작업하는 경우 (충돌 없음)

1. A가 `branch-A`에서 로그인 기능을 완성하고, 깃허브 `main` 브랜치에 코드를 먼저 올린다. (`main`이 업데이트됨)
2. 이후 B도 `branch-B`에서 장바구니 기능을 완성해서 깃허브에 코드를 올리려고 한다. 
3. 하지만 깃허브는 'A가 `main`을 이미 업데이트했으니, 그 코드부터 먼저 가져가`라며 B의 push를 거절한다.
4. B는 `git pull origin main`을 입력해, A가 만든 로그인 코드를 자기 브랜치로 가져온다. 
    <br>
    이때 A와 B가 작업한 폴더가 서로 겹치지 않기 때문에, Git이 알아서 두 코드를 충돌 없이 합쳐준다. 
6. 병합이 무사히 완료되면, B는 다시 안전하게 코드를 `push` 한다.

### 서로 같은 파일을 고쳤지만, 수정한 위치가 다른 경우 (충돌 없음)

1. A와 B 모두 `index.html` 파일을 열어서 작업한다.
2. A는 파일의 맨 위쪽에 로그인 버튼을 달고, 깃허브 `main` 브랜치에 코드를 먼저 올린다. (`main`이 업데이트됨)
3. 이후 B는 파일의 맨 아래쪽에 장바구니 버튼을 달고 깃허브에 코드를 올리려고 한다.
4. 하지만 깃허브는 'A가 `main`을 이미 업데이트했으니, 그 코드부터 먼저 가져가'라며 B의 push를 거절한다.
5. B는 `git pull origin main`을 입력해, A가 수정한 최신 코드를 자기 브랜치로 가져온다.
    <br>
    이때 둘 다 같은 파일을 건드렸지만 수정한 줄이 다르기 때문에, Git이 알아서 두 코드를 충돌 없이 합쳐준다.
6. 병합이 무사히 완료되면, B는 다시 안전하게 코드를 `push` 한다.

### 같은 파일의 같은 위치를 고친 경우 (충돌 발생)

1. A와 B 모두 `index.html`의 배경색 코드를 수정한다.
2. A는 배경색을 빨간색으로 수정하고, 깃허브 `main` 브랜치에 코드를 먼저 올린다. (`main`이 업데이트됨)
3. 이후 B는 자기 컴퓨터에서 배경색을 파란색으로 수정하고 깃허브에 코드를 올리려고 한다.
4. 하지만 깃허브는 'A가 `main`을 이미 업데이트했으니, 그 코드부터 먼저 가져가'라며 B의 push를 거절한다.
5. B는 `git pull origin main`을 입력해, A가 수정한 최신 코드를 자기 브랜치로 가져온다.
    <br>
    하지만 이번에는 같은 파일의 똑같은 줄을 서로 다르게 수정했기 때문에, Git이 어떤 색을 선택해야 할지 몰라 병합을 멈추고 **충돌 (Merge Conflict) 경고**를 띄운다.
6. B는 팀원과 상의하여 최종 배경색을 합의한 뒤, 충돌이 난 코드를 직접 알맞게 수정하고 저장한다.
7. 충돌을 직접 해결한 B는 다시 `git add`와 `commit`을 거친 뒤, 안전하게 코드를 `push` 한다.

### 내 작업이 안 끝났는데, main의 최신 코드가 당장 필요한 경우

1. A가 로그인 기능을 완성하여 깃허브 `main` 브랜치에 코드를 먼저 올린다. (`main`이 업데이트됨)
2. B는 `branch-B`에서 장바구니 기능을 만들던 중, A가 방금 올린 최신 로그인 코드가 당장 필요해졌다. 하지만 B의 작업은 아직 반밖에 끝나지 않았다.
3. 작업이 안 끝난 상태에서 남의 코드를 무작정 가져오면 코드가 꼬이거나 날아갈 수 있으므로, B는 현재까지 작성한 미완성 코드를 일단 `commit` 하여 컴퓨터에 안전하게 저장해둔다.
4. 이후 B는 `git pull origin main`을 입력해, A가 수정한 최신 코드를 자기 브랜치로 가져온다.
    <br>
    `commit`으로 코드를 저장했기 때문에, 만약 Git이 병합할 때 충돌하는 부분이 있다면 에러를 발생시키지 않고 사용자에게 물어보면서 병합하게 된다.
5. 최신 코드를 무사히 가져오면, B는 다시 작업을 계속 이어간다.
