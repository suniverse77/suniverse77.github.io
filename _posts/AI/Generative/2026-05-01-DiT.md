---
title: "[생성 모델] DiT (Diffusion Transformer)"
date: 2026-05-01 00:00:00 +/-TTTT
categories: [AI, 비전 생성 모델]
tags: [생성 모델, Diffusion]
math: true
toc: true
author: sunho
---

DiT는 기존 디퓨전 모델에서 표준처럼 사용되던 U-Net 백본을 Transformer 아키텍처로 대체한 모델이다.

DiT 모델의 파이프라인은 다음과 같다.

![fig1](AI/Generative/DiT-1.png){: style="display:block; margin:0 auto; width:100%;"}

DiT 또한 [LDM](https://suniverse77.github.io/posts/LDM/)처럼 latent 공간에서 동작하기 때문에, VAE 인코더를 거친 feature가 입력된다.

## Patchify

입력으로 들어온 노이즈가 섞인 latent $x_t \in \mathbb{R}^{C \times I \times I}$를 [ViT](https://suniverse77.github.io/posts/ViT/)와 동일한 방식으로 $p \times p$ 크기의 패치들로 분할한다.

이후 각 패치들을 Flatten하고 Linear Projection을 거쳐 1차원 토큰 시퀀스로 변환한다.

![fig2](AI/Generative/DiT-2.png){: style="display:block; margin:0 auto; width:70%;"}

## Positional Embedding

트랜스포머는 입력 토큰의 순서를 본질적으로 알 수 없기 때문에, 토큰 시퀀스에 위치 정보를 더해준다.

논문 원본에서는 2D Sine-Cosine 임베딩을 사용하였다.

## Conditioning 방식

논문에서는 Noise timestep $t$와 텍스트 프롬프트, 클래스 라벨과 같은 조건을 어떻게 주입하는 게 효과적인지 다양한 방법을 실험하였다.

논문에서 최종적으로 선택한 방법은 adaLN-Zero이다.

### In-Context Conditioning

timestep embedding과 class embedding을 transformer의 patch token sequence에 단순히 concat

- 장점: 기존 ViT 모델 구조를 바꿀 필요 없이 그대로 사용할 수 있다.
- 단점: Conditioning 정보가 다른 patch token과 완전히 똑같이 처리되므로, timestep이나 class signal을 강하게 주기 어렵다.

### Cross-Attention Block

DiT block에서 Self-Attention 뒤에 Cross-Attention layer를 추가해, Conditioning을 key와 value로, Input token을 query로 사용하는 방식이다.

- 장점: Conditioning 정보를 직접 attention으로 강하게 연결할 수 있다.
- 단점: Self-Attention만 쓸 때보다 연산량이 약 15% 늘어나고, 학습이 조금 더 불안정할 수 있다는 단점이 있다.

### adaLN-Zero (Adaptive LayerNorm with Zero-initialization)

#### Adaptive LayerNorm

transformer block 내부의 LayerNorm 수식은 다음과 같다.

$$
y=\gamma\cdot\frac{x-\mu}{\sqrt{\sigma^2+\epsilon}}+\beta
$$

여기서 $\gamma$와 $\beta$를 

$$
\gamma,\beta=\text{MLP}(\mathbf{c})
$$

즉, 이 MLP는 회귀하는 역할을 한다.

- 장점: Conditioning 정보를 DiT Block의 모든 feature에 고르게 적용할 수 있다.
- 단점: residual branch에 강하게 들어가면 학습 초기 불안정할 수 있다.

#### Zero-initialization
